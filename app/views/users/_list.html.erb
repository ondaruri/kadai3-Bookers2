

    <h2>User info</h2>
      <!--後で消す↓-->
      <%= attachment_image_tag user, :profile_image, :fill, size:'120x120', format: 'jpeg', fallback: "no_image.jpg" %>
      <table class="table">
          <thead>
          <tr>
            <th>name</th>
            <th><%= user.name %></th>
          </tr>
          <tr>
            <th>introduction</th>
            <th><%= user.introduction %></th>
          </tr>
          </thead>
      <tbody>
      </tbody>
      </table>
        <%= link_to "edit profile", edit_user_path(user), class: 'btn btn-outline-secondary btn-block fas fa-user-cog edit_user_10038' %>

<% if book.errors.any? %>
  <h2><%= pluralize(book.errors.count, "error") %> prohibited this obj from being saved:</h2>
  <% book.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>

<%= flash[:notice] %>

<h2 class="mt-3">New book</h2>
<%= form_with model: book, local: true do |f| %>
 <h4>title</h4>
  <%= f.text_field :title %>
  <h4>opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Create Book', class: 'btn btn-success' %>
<% end %>
